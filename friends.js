var human = {
    name: 'rahim',
    age: 25,
    friends: ['arshad', 'sharif', 'basir'],
    contry: 'bangladesh'
};

for ( var i = 0; i < human.friends.length; i++){
    const element = human.friends[i];
    console.log(element);
}